<div class="cover"></div>
<section class="section {{id}}">
  <div class="container">
    {{#if image }}
    <figure class="figure">
      {{{ picture image }}}
    </figure>
    {{/if}}
    <div class="content">
      <h1>{{title}}</h1>
      {{{content}}}
    </div>
  </div>
</section>