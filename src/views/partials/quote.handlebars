<blockquote class="quote{{#if dark }} dark{{/if}}">
  <p data-animate-in>
    <span class="text">{{ translateSanity quote.text @root.language }}</span>
    <span class="author">{{ quote.source }}</span>
  </p>
</blockquote>