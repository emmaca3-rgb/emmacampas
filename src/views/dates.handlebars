<div class="cover"></div>
<section class="section dates">
  <div class="container">
    <div class="content">
      <header>
        <h1>{{ title }}</h1>
        <p>{{ subtitle}} </p>
      </header>
      {{{content}}}
      <h2>{{ translate 'dates.upcoming' language }}</h2>
      <ul class="dates-list">
        {{#each dates}}
        <li>
          <time datetime="{{date}}">{{formatDate date @root.language}}</time>
          <span>{{description}}</span>
        </li>
        {{/each}}
      </ul>
    </div>
  </div>
</section>
